# CLAUDE.project.md — Project-Level Override

> This file extends the global `CLAUDE.md`. Only specify what differs from the global config.
> Drop this into any project root as `CLAUDE.project.md` (or merge into `CLAUDE.md` for simpler setups).
> Delete any section you don't need to override — silence means "inherit from global."

---

## Project Identity

- **Name**:
- **Type**: `[ ] SaaS  [ ] Enterprise App  [ ] Personal Tool  [ ] Library/SDK  [ ] CLI`
- **Domain**:
- **Repo**:

---

## Stack Overrides

> Replace only what differs from the global tech stack defaults in CLAUDE.md.

| Concern               | This Project            | Reason for Override |
|-----------------------|-------------------------|---------------------|
| Backend               |                         |                     |
| Frontend              |                         |                     |
| Database              |                         |                     |
| Auth                  |                         |                     |
| Testing               |                         |                     |
| Deployment            |                         |                     |
| *(add rows as needed)*|                         |                     |

---

## Architecture Constraints

> Hard constraints specific to this project that Claude must always respect.

- [ ] *(e.g., "All DB access must go through the repository layer — no direct ORM calls in controllers")*
- [ ] *(e.g., "No third-party dependencies without explicit approval — this is a zero-dependency library")*
- [ ] *(e.g., "All API responses must conform to the JSON:API spec")*
- [ ] *(e.g., "PII must never be logged — use redaction helpers in `src/utils/redact.ts`")*

---

## Domain Glossary

> Key terms, entities, and concepts specific to this project's domain.
> Claude should use these terms consistently in code, comments, and conversation.

| Term | Definition |
|------|-----------|
| *(e.g., Tenant)* | *(A paying customer organization with isolated data)* |
| *(e.g., Workspace)* | *(A sub-unit within a Tenant, maps to a team or project)* |
| | |

---

## Key Entry Points

> Files Claude should familiarize itself with at session start for this project.

```
# Core domain
src/domain/

# Main application service
src/application/

# API entry point
src/interfaces/http/router.ts

# DB schema
prisma/schema.prisma  (or migrations/)

# Environment config
.env.example
```

---

## Project-Specific Guardrails

> Additional rules beyond the global guardrails in CLAUDE.md.

- *(e.g., "Never modify files under `src/generated/` — these are auto-generated by the build pipeline")*
- *(e.g., "All Stripe webhook handlers must be idempotent — check event ID before processing")*
- *(e.g., "Feature flags must be registered in `src/config/flags.ts` before use")*

---

## Active Work Streams

> High-level epics or initiatives currently in progress. Helps Claude understand what's in flight
> without reading through full `CONTEXT.md` session history.

| Work Stream | Status | Owner | Notes |
|-------------|--------|-------|-------|
| | | | |

---

## Out of Scope (for this project)

> Things Claude should not do or suggest for this project, even if valid in general.

- *(e.g., "Don't suggest GraphQL — REST-only for this project")*
- *(e.g., "Don't introduce new infrastructure dependencies — we're in a cost-freeze")*

---

*Last updated: YYYY-MM-DD*